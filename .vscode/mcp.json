{
    "servers": {
        "filesystem": {
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-filesystem",
                "/home/levente/projects/discount-hunter"
            ]
        },
        "git": {
            "command": "${workspaceFolder}/.venv/bin/uvx",
            "args": [
                "mcp-server-git",
                "--repository",
                "${workspaceFolder}"
            ]
        }
    }
}